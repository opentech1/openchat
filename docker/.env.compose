# Default Compose credentials. Override in production or regenerate with:
#   bun run generate:secrets --force
POSTGRES_USER=ochat_app
POSTGRES_PASSWORD=change-me-dev-password
POSTGRES_DB=openchat
DATABASE_URL=postgres://ochat_app:change-me-dev-password@postgres:5432/openchat
SHADOW_DATABASE_URL=postgres://ochat_app:change-me-dev-password@postgres:5432/openchat_shadow
PG_PROXY_PASSWORD=change-me-dev-proxy
ELECTRIC__PG_PROXY__PASSWORD=change-me-dev-proxy

# Fill these with strong, unique secrets before deploying.
ELECTRIC_GATEKEEPER_SECRET=replace-with-strong-secret
ELECTRIC_SECRET=replace-with-strong-secret
BETTER_AUTH_SECRET=replace-with-strong-secret
