{
  "name": "openchat",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "check": "turbo lint",
    "dev": "turbo -F web-start -F server dev",
    "dev:web-start": "turbo -F web-start dev",
    "build": "turbo build",
    "build:web-start": "turbo -F web-start build",
    "check-types": "turbo check-types",
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "test:web": "vitest run apps/web/test",
    "test:server": "vitest run apps/server/test",
    "dev:native": "turbo -F native dev",
    "dev:web": "turbo -F web dev",
    "dev:server": "turbo -F server dev",
    "dev:extension": "turbo -F extension dev",
    "build:extension": "turbo -F extension build",
    "db:push": "turbo -F server db:push",
    "db:studio": "turbo -F server db:studio",
    "db:generate": "turbo -F server db:generate",
    "db:migrate": "turbo -F server db:migrate",
    "setup:db": "bun run scripts/setup-db.ts",
    "generate:secrets": "bun run scripts/generate-secrets.ts",
    "verify:build": "bunx turbo run build --filter=web-start --filter=server"
  },
  "dependencies": {
    "pg": "^8.16.3",
    "posthog-node": "^5.9.5"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "@vitest/coverage-v8": "^3.2.4",
    "oxlint": "^1.22.0",
    "turbo": "^2.5.8",
    "vitest": "^3.2.4",
    "ws": "^8.18.0"
  },
  "packageManager": "bun@1.2.21"
}
