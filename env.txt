# --- Core URLs ---
NEXT_PUBLIC_APP_URL=https://ochat.pro
NEXT_PUBLIC_SERVER_URL=https://api.ochat.pro
BETTER_AUTH_URL=https://api.ochat.pro
AUTH_COOKIE_DOMAIN=.ochat.pro

# --- Better Auth ---
BETTER_AUTH_SECRET=change-me-super-long-random-value

# --- Database ---
DATABASE_URL=postgres://postgres:postgres@postgres:5432/openchat
SHADOW_DATABASE_URL=postgres://postgres:postgres@postgres:5432/openchat_shadow

# --- ElectricSQL ---
ELECTRIC_SERVICE_URL=http://electric:3010
ELECTRIC_GATEKEEPER_SECRET=prod-gatekeeper-secret
PG_PROXY_PASSWORD=prod-proxy-password
ELECTRIC_INSECURE=false

# --- Web client sync ---
NEXT_PUBLIC_ELECTRIC_URL=https://electric.ochat.pro
NEXT_PUBLIC_ELECTRIC_WORKSPACE_TTL_MS=600000
NEXT_PUBLIC_ELECTRIC_CHAT_TTL_MS=300000
NEXT_PUBLIC_ELECTRIC_MAX_WORKSPACES=4
NEXT_PUBLIC_ELECTRIC_MAX_CHAT_COLLECTIONS=12

# --- OpenRouter ---
OPENROUTER_API_KEY_SECRET=change-me-too
OPENROUTER_BASE_URL=
OPENROUTER_SCOPE=

# --- Development toggles (set to 0 in production) ---
DEV_ALLOW_HEADER_BYPASS=0
NEXT_PUBLIC_DEV_BYPASS_AUTH=0
NEXT_PUBLIC_DEV_USER_ID=

# --- Networking ---
CORS_ORIGIN=https://ochat.pro
SERVER_INTERNAL_URL=http://server:3000
TRUST_PROXY_FORWARDED=1
RATE_LIMIT_PER_MIN=60

# --- Ports ---
SERVER_PORT=3000
WEB_PORT=3001
ELECTRIC_HTTP_PORT=3010
ELECTRIC_PROXY_PORT=5133
