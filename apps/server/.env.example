# Convex environment template (`apps/server/.env.local`)
# Copy to apps/server/.env.local and fill in secrets before running Docker Compose.

# Example: Postgres connection string if your Convex functions talk to a database.
# DATABASE_URL=postgresql://user:pass@host:5432/db

# Optional: expose the dashboard through a reverse proxy.
# CONVEX_ADMIN_URL=https://convex.example.com

# Mark deployment as local (Convex dev server only)
CONVEX_DEPLOYMENT=local
