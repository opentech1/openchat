CORS_ORIGIN=http://localhost:3001
BETTER_AUTH_URL=http://localhost:3000
BETTER_AUTH_SECRET=dev-secret
DATABASE_URL=postgres://user:password@localhost:5432/openchat

# GitHub OAuth
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# SMTP (Email OTP)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="OpenChat <no-reply@openchat.local>"

# Frontend origin used to build links in OTP emails (falls back to CORS_ORIGIN)
FRONTEND_URL=

# OTP Rate limiting (defaults shown)
OTP_RATE_WINDOW=3600
OTP_RATE_LIMIT_PER_EMAIL=3
OTP_RATE_LIMIT_PER_IP=20
