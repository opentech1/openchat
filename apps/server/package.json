{
  "name": "server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "convex dev",
    "build": "if [ \"$NEXT_PUBLIC_DEPLOYMENT\" = \"preview\" ]; then convex deploy --cmd-url-env-var-name NEXT_PUBLIC_CONVEX_URL --cmd 'cd ../.. && bun run build:web' --preview-run previewSeed --preview-create $VERCEL_GIT_COMMIT_REF; else convex deploy --cmd-url-env-var-name NEXT_PUBLIC_CONVEX_URL --cmd 'cd ../.. && bun run build:web'; fi",
    "check-types": "tsc --noEmit || true",
    "test": "echo \"No Convex tests yet\""
  },
  "dependencies": {
    "@convex-dev/better-auth": "^0.9.7",
    "convex": "1.28.2"
  }
}
